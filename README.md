# VeryUnsafeBlog.com

A simple "blog" with a backend written in Java and a frontend written in Svelte.

Allows for creating posts with support for Markdown.
The reason this is called an "unsafe" blog is because there is no login system and it comes with no safety measures and it can't really be moderated.
